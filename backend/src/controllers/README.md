# controllers/

Controladores por recurso (ej. `catalogo.controller.js`, `prestamo.controller.js`,
`usuario.controller.js`, `auth.controller.js`). Reciben la petición, delegan en
`services/` y devuelven la respuesta.

_Placeholder de estructura._
