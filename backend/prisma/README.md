# prisma/

En un desarrollo real contendría el `schema.prisma` (modelos, datasource
PostgreSQL) y las migraciones generadas por Prisma. Como este proyecto es
solo una simulación del flujo de Git/GitHub y no se va a programar, ese
esquema se documenta en Markdown en vez de escribirse como código real.

## Estado actual

Se agregó `modelos.md`, un boceto en texto de los modelos base que tendría
el esquema: `Usuario`, `Libro`, `Catalogo` y `Prestamo`. Describe campos y
relaciones a alto nivel, sin sintaxis de Prisma ni ejecución real.
